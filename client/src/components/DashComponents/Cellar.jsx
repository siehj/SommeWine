import React from 'react';

const Cellar = (props) => {
  return (
    <div>
      <h2 className="tabTitle">Cellar</h2>
      {/* show user's Cellar saved to the DB */}
    </div>
  )
}

export default Cellar;